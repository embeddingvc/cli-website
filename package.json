{
  "name": "embeddingvc-cli-website",
  "version": "0.1.0",
  "description": "Embedding VC's geeky website",
  "dependencies": {
    "aalib.js": "^2.0.0",
    "ai": "^4.3.10",
    "netlify-cli": "^20.1.1",
    "xterm": "^4.10.0",
    "xterm-addon-fit": "^0.5.0",
    "xterm-addon-web-links": "^0.4.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git@github.com:embeddingvc/cli-website.git"
  },
  "scripts": {
    "build": "npm install && mkdir -p ./css && cp node_modules/xterm/css/*.css ./css && mkdir -p ./js && cp node_modules/xterm*/lib/*.js* ./js && cp node_modules/aalib.js/dist/aalib.js* ./js",
    "start": "netlify dev"
  }
}
